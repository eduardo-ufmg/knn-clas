# USE C++20 OR HIGHER
# INCLUDE PROTOBUF HEADERS
# LINK PROTOBUF LIBRARIES
# COMPILE `source/` DIRECTORY `.cpp` AND `.cc`
# BUILD LABEL FROM `label.cpp`
# BUILD TRAIN FROM `train.cpp`
# BINARIES GO TO `bin/`
